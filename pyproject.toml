[project]
name = "scouter"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["aiohttp>=3.9", "pandas>=2.2", "numpy>=1.26"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
